name: Deploy Theme via SFTP (sftpcloud)

permissions:
  contents: read

on:
  push:
    branches: [ "main" ]
    paths:
      - "**/*"
      - "!.github/**"
      - ".github/workflows/deploy-sftp.yml"
  workflow_dispatch: {}


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload via SFTP (SamKirkland sftp-deploy)
        uses: SamKirkland/sftp-deploy@v1.4.0
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          port: ${{ secrets.SFTP_PORT || '22' }}
          local-path: .
          remote-path: /ai-news-hub
          exclude: |
            **/.git/**
            **/.DS_Store
            **/node_modules/**
            **/src/**
            **/.vite/**
            **/coverage/**
            **/*.map 
 